<div class="row">
	{{#each technologies}}
		<div class="col-lg-2 col-md-3 col-sm-4 col-xs-6 text-center">
			<img class="img-fluid logo-img" src="./images/logos/{{image}}.png">
			<h6 class="logo-title">{{name}}</h6>
	</div>
	{{/each}}
</div>